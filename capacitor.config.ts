import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shiguang.kebiao',
  appName: '拾光课表',
  webDir: 'outputs',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#f6f7fb'
  }
};

export default config;
